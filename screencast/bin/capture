#!/bin/bash

# Check params
[[ $# != 1 ]] && echo usage: "$0 project" && exit

project=$1

# Create a directory for this project
mkdir -p $project
pushd $project

echo Starting key capture to \"$project\"
echo "^D to terminate caputre"
sleep 1
clear

# Capture
script -t --timing=cast.timing cast
